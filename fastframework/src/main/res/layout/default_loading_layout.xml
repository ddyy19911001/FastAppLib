<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/status_layout_background_color"
    android:gravity="center"
    android:orientation="vertical">

    <ProgressBar
        android:visibility="gone"
        android:id="@+id/pb_status_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:visibility="gone"
        android:id="@+id/tv_status_loading_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:text="@string/status_layout_manager_loading"
        android:textColor="@color/status_layout_tip_text_color"
        android:textSize="16sp" />
    <com.cunoraz.gifview.library.GifView
        android:id="@+id/gif_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        custom:gif="@drawable/loading" />
</LinearLayout>
